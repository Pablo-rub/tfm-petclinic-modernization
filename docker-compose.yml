services:
  vets-service:
    build: ./vets-service
    ports:
      - "8081:8081"

  petclinic:
    build: .
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m
      - vets.service.url=http://vets-service:8081
    depends_on:
      - vets-service
